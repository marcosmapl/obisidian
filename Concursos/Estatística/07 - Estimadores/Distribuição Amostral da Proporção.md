
É possível demonstrar que a proporção amostral é uma variável aleatória com média, variância e desvio padrão dados por:

$$ E(\hat{p}) = p $$

$$ VAR(\hat{p}) = \frac{p \cdot q}{n} $$

$$ DP(\hat{p}) = \sqrt{\frac{p \cdot q}{n}} $$
